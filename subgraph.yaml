specVersion: 0.0.4
description: JUMBO
repository: https://github.com/jumbo-exchange/subgraph
schema:
  file: ./schema.graphql
dataSources:
  - kind: near
    name: ref-contract
    network: near-testnet
    source:
      account: ref-contract.solniechniy.testnet
      startBlock: 75324040
    mapping:
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      file: ./src/mapping.ts
      entities:
        - Account
        - Log
      receiptHandlers:
        - handler: handleReceipt