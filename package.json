{
  "name": "jumbo-exchange",
  "version": "0.1.0",
  "repository": "https://github.com/jumbo-exchange/subgraph.git",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy $NAME --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.28.0",
    "@graphprotocol/graph-ts": "^0.25.0",
    "assemblyscript-json": "^1.1.0"
  },
  "dependencies": {
    "assemblyscript-regex": "^1.6.4",
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0"
  }
}
